# Erotica Barber & Massage — POS System Overview
> เอกสารสรุปการทำงานของระบบ POS สำหรับร้านตัดผมและนวดเพื่อสุขภาพ

---

## 📁 Directory Tree

```
โปรเจค POS/
├── index.html              # UI หลัก — ทุกหน้าอยู่ในไฟล์เดียว (SPA)
├── app.js                  # Logic ทั้งหมด (class PosApp, ~4,400 บรรทัด)
├── style_v2.css            # Dark Premium CSS Design System (~1,900 บรรทัด)
├── sw.js                   # Service Worker — cache offline (v40)
├── manifest.json           # PWA manifest — ติดตั้งบนมือถือได้
├── vendor/                 # ไฟล์ภายนอกที่เก็บไว้เอง (ห้ามกลับไปโหลดจาก CDN)
│   ├── fontawesome/        #   ไอคอน FA 6.4.0 (จาก npm)
│   └── fonts/              #   ฟอนต์ Sarabun + Outfit + app-fonts.css
├── google_apps_script.js   # Backend บน Google Sheets (deploy เป็น Web App)
└── ลิ้งข้อมูลต่างๆ/
    └── ข้อมูลลิ้งค์ต่างๆ.txt
```

---

## 🏗 สถาปัตยกรรมระบบ

```
┌─────────────────────────────────────────────────────────┐
│                    Browser / PWA                        │
│                                                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │  index.html  │  │ style_v2.css │  │    sw.js     │  │
│  │  (6 screens) │  │  (Dark UI)   │  │ (Offline SW) │  │
│  └──────┬───────┘  └──────────────┘  └──────────────┘  │
│         │                                               │
│  ┌──────▼───────────────────────────────────────────┐   │
│  │                   app.js (PosApp class)          │   │
│  │                                                  │   │
│  │  State ──► Dexie.js (IndexedDB)                  │   │
│  │  ├── services, staff, customers                  │   │
│  │  ├── transactions, queue, shift                  │   │
│  │  └── settings (PIN hash, PromptPay, Sheets URL)  │   │
│  └──────────────────────┬───────────────────────────┘   │
└─────────────────────────┼───────────────────────────────┘
                          │ HTTPS POST (fetch)
          ┌───────────────▼───────────────┐
          │    Google Apps Script (GAS)   │
          │    deployed as Web App        │
          │                               │
          │  action = "transaction"       │
          │    └─► บันทึกบิล → MM-yyyy   │
          │                               │
          │  action = "summary_day"       │
          │    └─► สร้าง สรุป-YYYY-MM-DD │
          │                               │
          │  action = "summary_month"     │
          │    └─► สร้าง สรุป-MM-yyyy    │
          │    └─► อัปเดต สรุปรายเดือน  │
          └───────────────────────────────┘
                          │
          ┌───────────────▼───────────────┐
          │       Google Sheets           │
          │  ├── สรุปรายเดือน (master)   │
          │  ├── 06-2026 (บิลรายการ)     │
          │  ├── สรุป-06-2026 (monthly)  │
          │  └── สรุป-2026-06-06 (daily) │
          └───────────────────────────────┘

          ┌───────────────────────────────┐
          │     Telegram Bot API          │
          │  └─► รายงานสรุปปิดกะ         │
          └───────────────────────────────┘
```

---

## 🔄 Data Flow หลัก

### 1. เปิดร้าน
```
เปิดแอป → [บล็อกหน้าจอ] → นับเงินสดเปิดกะ → บันทึก startCash + startTime → เข้าใช้งานได้
```

### 2. ขาย (POS)
```
เลือกบริการ → เลือกพนักงาน → ระบุลูกค้า → ส่วนลด
→ เลือกวิธีชำระ (เงินสด / QR / Credit)
→ processCheckout()
  ├── สร้าง Transaction (TX-{timestamp}-{8chars})
  ├── สร้าง Queue item (waiting)
  ├── saveState() → Dexie.js (IndexedDB)
  ├── syncPendingTransactions() → GAS [background]
  ├── พิมพ์ใบเสร็จ
  └── lazy render (dashboard + pos + queue)
```

### 3. คิวงาน
```
Queue: waiting → [startQueue] → serving [timer] → [completeQueue] → ลบออก
```

### 4. ปิดร้าน
```
นับเงินสดปิดกะ → เปรียบเทียบ expected vs counted → บันทึก shiftLog
→ sendTelegramReport()    [Telegram Bot]
→ syncDailySummary()      [GAS → สรุป-YYYY-MM-DD]
→ syncMonthlySummary()    [GAS → สรุป-MM-yyyy + สรุปรายเดือน]
→ รีเซ็ต queue + cart → เปิดหน้านับเงินใหม่
```

---

## 📱 6 หน้าหลัก

| หน้า | ฟีเจอร์หลัก |
|------|-------------|
| **Dashboard** | KPI วันนี้, คิวงาน live, ยอดขายล่าสุด, บริการยอดนิยม, ค่าใช้จ่ายรายวัน |
| **POS** | เลือกบริการ, ตะกร้า, เลือกพนักงาน, ส่วนลด, ชำระเงิน 3 ช่องทาง |
| **คิวงาน** | รอ / กำลังบริการ, progress bar countdown, เริ่ม/เสร็จ/ยกเลิก |
| **ลูกค้า** | ค้นหา, ระบบ tier (General/Gold/Platinum), note พิเศษ |
| **รายงาน** | กรองวัน/เดือน, กรองพนักงาน, commission, บริการยอดขาย, ส่งสรุป Sheets |
| **ตั้งค่า** | บริการ, พนักงาน, PromptPay, PIN, Google Sheets URL + รหัสเชื่อมต่อ, Telegram |

---

## 🔐 ระบบความปลอดภัย

| จุด | วิธีการ |
|-----|---------|
| PIN เจ้าของร้าน | SHA-256 hash ก่อนเก็บ — ไม่มี plain text ใน IndexedDB |
| Migration | PIN เก่า (plain text) → hash อัตโนมัติตอนเปิดแอปครั้งแรก |
| Role control | staff (default) / owner (ต้องใส่ PIN) — รายงาน+ตั้งค่า owner only |
| Telegram token | เก็บใน IndexedDB (persistent ข้ามวัน) |
| GAS sync | HTTPS POST + LockService ป้องกัน race condition |

---

## ☁️ Google Sheets Structure

```
สรุปรายเดือน (sheet แรก — master)
├── ประเภท | ช่วงเวลา | บิล | รายได้รวม | ค่าใช้จ่าย | กำไรสุทธิ | อัปเดตล่าสุด
├── รายเดือน | 05-2026 | 128 | ...
├── รายเดือน | 06-2026 | 47  | ...
├── รายวัน   | 2026-06-05 | 8 | ...
└── รายวัน   | 2026-06-06 | 9 | ...

สรุป-06-2026 (monthly snapshot)
├── Section: KPI 5 ตัว (รายได้, ค่าใช้จ่าย, กำไร, บิล, เฉลี่ย/บิล)
├── Section: ช่องทางชำระเงิน (เงินสด / QR / Credit)
├── Section: รายการบริการ (จำแนกตามยอดขาย + %)
├── Section: รายละเอียดค่าใช้จ่าย + รวม
├── Section: สรุปกำไรสุทธิ (รายได้ − ค่าใช้จ่าย)
└── Section: ค่าคอมมิชชั่นพนักงานรายบุคคล

สรุป-2026-06-06 (daily snapshot) — เนื้อหาเดียวกัน เฉพาะวันนั้น

06-2026 (transaction detail) — 13 คอลัมน์
└── เลขที่บิล | วันที่-เวลา | ลูกค้า | บริการ | ช่องทาง | ราคารวม | ส่วนลด
    | ไม่คิด VAT | คิด VAT | VAT | ปัดเศษ | ยอดสุทธิ | พนักงาน

    บวกลงตัวจากซ้ายไปขวา:  ราคารวม − ส่วนลด = ไม่คิด VAT + คิด VAT
                          ไม่คิด VAT + คิด VAT + VAT + ปัดเศษ = ยอดสุทธิ

    แท็บที่สร้างก่อน v1.5.6 มี 9 คอลัมน์ (ไม่มีช่อง VAT)
    migrateBillSheetAddVatColumns_() เติมให้เองตอนเขียนบิลใบถัดไป
    แถวบิลเก่าเว้นว่างใน 4 ช่องใหม่ — จงใจ ไม่เดาย้อนหลัง
```

---

## ⚡ PWA & Offline

```
Service Worker (jahn-pos-v40)
├── install   → cache: index.html, app.js, style_v2.css, dexie.min.js, promptpay-qr.js, manifest.json
├── activate  → ลบ cache เก่า
└── fetch     → cache-first → fallback to network → fallback to index.html

ทำงานออฟไลน์ได้ทันที — ข้อมูลอยู่ใน Dexie.js (IndexedDB)
Sync ขึ้น Sheets เมื่อมี internet (pending → synced)
```

---

## ⚠️ จุดอ่อนที่พบ และสถานะการแก้ไข

### ✅ แก้ไขแล้ว

| จุดอ่อน | ปัญหา | วิธีแก้ |
|---------|--------|---------|
| **TX ID ซ้ำ** | ใช้ `length+1` ทำให้ซ้ำหลัง void | `TX-{timestamp}-{8char random}` |
| **PIN plain text** | เห็นได้ใน DevTools | SHA-256 hash ก่อนเก็บ |
| **GAS sync ไม่รู้ผล** | `no-cors` → ไม่รู้ว่าสำเร็จหรือเปล่า | ลบ no-cors + ตรวจ response.ok |
| **ใบเสร็จราคาผิด** | แสดง subtotal แทนราคาต่อรายการ | ใช้ `tx.details[i].price` |
| **กู้ข้อมูลเก่าไม่ได้** | Auto-archive เคยลบประวัติหลังเวลาผ่านไป | ปิดการลบอัตโนมัติ; วางแผน Export/เก็บถาวรเองเมื่อข้อมูลโต |
| **GAS merge crash** | cells ซ้อนกัน → script หยุด | แก้ column range ให้ถูกต้อง |
| **GAS KPI สีผิด** | กำไรติดลบยังแสดงเขียว | ตรวจ `netIncome >= 0` |
| **alert() blocking** | หน้าจอค้างทุกครั้ง | Toast notification system |
| **ไม่ validate phone** | กรอกผิดรูปแบบได้ | regex `/^0[0-9]{8,9}$/` |
| **ลูกค้าซ้ำได้** | เบอร์เดียวกัน 2 คน | Duplicate phone check |
| **addService ไม่ validate** | ราคา/เวลา = 0 ได้ | ตรวจ price > 0, duration > 0 |

### 🟡 จุดอ่อนที่ยังเหลือ (แนะนำแก้ในอนาคต)

| จุด | ผลกระทบ | แนวทาง |
|-----|---------|---------|
| **ข้อมูลไม่ real-time ข้ามอุปกรณ์** | แต่ละเครื่องข้อมูลแยกกัน + sync เป็นทางเดียว (ขึ้น Sheets เท่านั้น) | ใช้เครื่องเดียวก่อน / ย้ายไป Firebase Firestore ถ้าต้องหลายเครื่อง |
| **รหัสเชื่อมต่ออยู่บน iPad** | คนที่เข้าถึงเครื่องปลดล็อกอาจดึงรหัสออกได้ | รหัสไม่อยู่ใน GitHub/ไฟล์สำรองแล้ว; สงสัยรั่วให้รัน `rotatePosApiToken` แล้วเปลี่ยนทุกเครื่อง |
| **ข้อมูลบวมขนาดใหญ่** | ข้อมูลสะสมเยอะเกินไป | ไม่ลบประวัติอัตโนมัติแล้วเพื่อกันกู้ข้อมูลเก่าไม่ได้; ต้องวางแผน export/เก็บถาวรเมื่อข้อมูลโต |
| **ไม่มีระบบ backup อัตโนมัติเต็มรูปแบบ** | auto-backup ทำตอนปิดกะ + ต้องตั้ง GAS URL และรหัสเชื่อมต่อ; ถ้า browser clear ก่อนหน้าอาจหาย | กด Export JSON เองสม่ำเสมอ |
| **ไม่มี rate limiting** | ส่ง GAS ซ้ำๆ ถ้าเครือข่ายไม่เสถียร | retry with exponential backoff |

> ✅ **แก้แล้ว (มิ.ย. 2569):** QR PromptPay เป็น EMVCo มาตรฐานจริง (สแกนจ่ายได้) · ตัด fallback เบอร์พร้อมเพย์ปลอม + validate · ค่าคอมคิดบนยอดหลังหักส่วนลด · ใบเสร็จเก็บเงินรับ-ทอน · void รีเฟรชชีตสรุป · custom modal แทน confirm/alert/prompt · ล้างข้อมูล demo · apple-touch-icon สำหรับ iOS
>
> ✅ **แก้แล้ว (ส.ค. 2569 — ซ่อมของที่เพิ่งเพิ่มเข้าไปเอง, v1.5.4):**
> **กล่องล็อกอินไปซ้อนใต้หน้าต่างอื่น** — `modal-login` เป็นตัวแรกใน HTML ทุกหน้าต่างใช้ z-index 200 เท่ากัน ตัวหลังจึงทับตัวหน้า พอมี auto-logout 5 นาที (v1.5.2) การเตะออกเกิดได้ทุกวินาที รวมถึงตอนหน้าต่างชำระเงิน/นับเงินปิดกะเปิดค้าง → เห็นกล่องล็อกอินแต่กดช่อง PIN ไม่ได้ = ค้างทั้งเครื่อง แก้ 2 ชั้น: `requireLogin()` ปิดหน้าต่างอื่นทิ้งก่อน + `#modal-login { z-index: 300 }`
> **ใบเสร็จยาวถูกตัดตอนพิมพ์** — `max-height` + `overflow-y:auto` ที่ใส่ให้ `.modal-card` (v1.5.3 กันปุ่มตกขอบจอเตี้ย) มีผลตอนพิมพ์ด้วย บล็อก `@media print` ปลดล็อกไว้แค่ `#thermal-receipt-preview` ยังไม่ครอบ `.modal-card` → บิล 14 รายการพิมพ์ออกมาขาดท้าย เพิ่ม `max-height:none !important; overflow:visible !important`
> **ห้ามเตะออกกลางการกู้ข้อมูล** — `checkIdleTimeout()` ข้ามเมื่อ `restoreBusy` เป็น true
> **เทสต์กันย้อนกลับ:** `tests/uiguard.js` (9 ข้อ) ครอบทั้ง 3 เคส · เพิ่มใน `run-all-ui.js` แล้ว
> **เอกสารกู้ข้อมูลฉุกเฉิน** อัปเดตให้ตรงกับของจริง — เดิมบอกให้ไปหาไฟล์ที่ดาวน์โหลด ทั้งที่ตอนนี้มีปุ่ม "ย้อนกลับไปก่อนกู้ข้อมูล" กดทีเดียวจบ
>
> ✅ **แก้แล้ว (ส.ค. 2569 — เลย์เอาต์หลายขนาดหน้าจอ, v1.5.3):**
> **ตะกร้าไม่หลุดจอบน iPad แนวนอนอีกแล้ว** — `.pos-layout > * { min-width: 0 }` (บวก `.category-tabs`) เดิมช่องซ้ายไม่ยอมหดตามค่า min-width:auto ของกริด แถบหมวดหมู่ที่ยาวเกินจอเลยดันตะกร้า+ปุ่มเก็บเงินตกขอบทั้งอัน — วัดจริง: iPad mini นอนล้น 497px · iPad 10.9 นอน 341px · โน้ตบุ๊ก 1280 ล้น 242px · iPad Pro 12.9 **แนวตั้ง** ก็โดน 497px
> **ปุ่มได้ขนาดนิ้วสัมผัส** — ขยาย `@media (pointer: coarse)` ให้ปุ่ม/ช่องกรอกขั้นต่ำ 44px (เดิมช่องเลือกพนักงานในตะกร้าสูง 20px · ปุ่มลบรายการ 23×20px) จอที่ใช้เมาส์ไม่เปลี่ยน · ปรับ `.cart-item-staff` ให้ตัดบรรทัดได้เมื่อช่องแคบ
> **มือถือแนวนอนกดเมนูได้แล้ว** — ทุกเงื่อนไข media query ที่เปิดแถบเมนูข้างเพิ่ม `and (min-height: 500px)` เดิมจอ 844×390 เข้าเงื่อนไข "กว้างพอ" เลยได้แถบข้างแบบคอม แต่เมนู "ตั้งค่า" ตกไปที่ y=501 คือใต้ขอบจอ และแถบล่างถูกซ่อนไปแล้ว = เปลี่ยนหน้าไม่ได้เลย
> **หน้าต่างป๊อปอัปเลื่อนดูได้บนจอเตี้ย** — `.modal-card` เพิ่ม `max-height: calc(100dvh - 2.5rem)` + `overflow-y: auto` เดิมหน้าต่างชำระเงิน/เพิ่มพนักงาน/เพิ่มบริการ ยาวเลยขอบจอทั้งบนล่างบนจอสูง 390px แล้วเลื่อนไม่ได้ = กดปุ่มยืนยันไม่ถึง
> **จอ 1024-1279px ไม่อึดอัด** — แถบข้าง 270px เลื่อนไปเริ่มที่ 1280px ช่วง iPad แนวนอนคงไว้ 225px คืนพื้นที่ให้การ์ดบริการ (188px → 258px, ชื่อบริการจาก 3 บรรทัดเหลือ 2) · `.cart-items` เปลี่ยนจากตายตัว 300px เป็น `clamp(220px, 38vh, 520px)`
> **ชื่อร้านบนแถบข้างไม่ถูกตัดหาย** — เดิม `white-space: nowrap` ไม่มี overflow ชื่อยาวหายไปเฉย ๆ ตอนนี้ตัดได้ 2 บรรทัด ย่อขนาดตามความกว้างจริง เกินนั้นมีจุดไข่ปลา
> **ทดสอบ:** `tests/responsive.js` (13 ขนาด × 6 หน้า) · `tests/modaltest.js` (7 หน้าต่าง × 6 ขนาด) · `tests/density.js` (ความอึดอัดของการ์ด)
>
> ✅ **แก้แล้ว (ส.ค. 2569 — audit รอบ 4, v1.5.2):**
> **ล็อกอินเจ้าของร้านหมดอายุใน 5 นาที** — `OWNER_IDLE_TIMEOUT_MINUTES` นับจากการแตะจอครั้งสุดท้าย (ไม่ใช่ตั้งแต่ตอนล็อกอิน) จึงไม่เตะออกกลางงาน · พนักงาน/ผู้จัดการยังใช้ `SESSION_TTL_HOURS` = 20 ตามเดิม · โดนเตะแล้วระบบเลือกผู้ใช้คนเดิมไว้ให้ พิมพ์ PIN ต่อได้เลย · `tryRestoreSession` ใช้ TTL ตามสิทธิ์
> **ตรวจไฟล์สำรองแบบละเอียด** — `auditBackupData` จับบิลยอดเงินหาย/วันที่พัง/ไม่มีเลขที่/เลขซ้ำ/ค่าใช้จ่ายพัง แล้วรายงานเป็นภาษาคนในกล่องยืนยันก่อนเขียนทับ (ทั้งนำเข้าไฟล์และกู้จาก Drive) · `sanitizeBackupData` แปลงตัวเลขที่เป็นข้อความให้ถูกต้อง และตั้งค่าที่พังจริงเป็น 0 เพื่อกัน NaN ลามจนยอด "ทั้งวัน" หายไปพร้อมบิลดีทุกใบ · `cashReceived/cashChange` ที่เป็น null ไม่ถูกยัด 0 (ไม่งั้นบิลโอนจะโชว์ช่องเงินทอน) · `showConfirm` รับ `onCancel` แล้ว มี `askConfirm` แบบ await ได้
> **ฝั่ง Sheets ตรวจค่าคอมและรายการบริการ** — `normalizeStaffCommissions_` / `normalizeServiceRows_` เพิ่มเข้ามาให้ครบเท่าบล็อกอื่น (เดิม staffCommissions ผิดรูป = `.sort()` พังทั้งงาน และ services ผิดรูปกลายเป็น 0 เงียบ ๆ) · กัน formula injection ที่ชื่อด้วย
> **ไม่พึ่งไฟล์จากเว็บนอกอีกแล้ว** — Font Awesome 6.4.0 และฟอนต์ Sarabun/Outfit ย้ายมาไว้ใน `vendor/` (ที่มาจาก npm ไม่ดัดแปลง) · ถอด `@import` จาก fonts.googleapis.com ที่บรรทัดแรกของ `style_v2.css` ทิ้ง · `sw.js` แคชครบทั้งชุด (ตัวประกาศฟอนต์อยู่ CORE, ไฟล์ฟอนต์เป็น best-effort กันติดตั้งล้มทั้งยวง) · ⚠️ เพิ่ม `vendor` ใน `$safeNewPaths` ของ `deploy.ps1` แล้ว — ถ้าลืม ไฟล์จะไม่ถูก push แล้วไอคอนบนเว็บจริงหายหมดโดยสคริปต์ไม่เตือน · ทดสอบจริงในเบราว์เซอร์: ไม่มีคำขอวิ่งออกนอกเครื่องเลย และตัดเน็ตแล้วไอคอน+ฟอนต์ไทยยังขึ้นครบ
> **ช่องนับเงินกันค่าติดลบ** — `readCashQty()` clamp เป็นจำนวนเต็ม 0-99999 แล้วเขียนค่ากลับลงช่องให้เห็น ใช้ตัวเดียวกันทั้งตอนคำนวณโชว์และตอนกดยืนยัน
> **ลบโค้ดที่ไม่ถูกเรียกใช้** — `archiveOldData()`, `sendTelegramReport()`, `sendVoidAlert()` (งานจริงเดินผ่าน `cloudOutbox` ซึ่ง retry ได้) · เพิ่ม `<link rel="icon">` กันเบราว์เซอร์ยิงขอ favicon แล้วได้ 404 ทุกครั้ง
>
> ✅ **แก้แล้ว (ส.ค. 2569 — audit รอบ 3, v1.5.1):**
> **บิลเก่าไม่ถูกแก้ตอนแค่เปิดดู** — หน้าต่างแก้ไขบิลทำงานบน "ร่าง" (`_editTxDraft`) แยกจากบิลจริง เขียนลงบิลเฉพาะตอนกดบันทึกเท่านั้น · บิลเก่าที่ไม่มีรายละเอียดรายชิ้น ระบบเดาราคาโดยใช้ราคาปัจจุบันเป็น "สัดส่วน" แล้วเกลี่ยให้ผลรวมเท่ายอดเดิมเป๊ะ (เดิมยัดราคาวันนี้ตรง ๆ ทำให้ยอดบิลเก่าเปลี่ยนตามราคาที่ขึ้น) · รายละเอียดที่สร้างให้มี `netPrice` ติดมาด้วย รายงาน/ชีตจึงไม่ใช้ราคาเต็มจนยอดขายกับค่าคอมโป่ง · บิลรุ่นก่อน VAT (`isLegacyBill`) ไม่ถูกกฎ "ปัดขึ้นเต็มบาท" ย้อนหลัง · พนักงานที่ถูกลบไปแล้วมีตัวเลือก "คงไว้ตามเดิม" ค่าคอมไม่ถูกโยนให้คนแรกในลิสต์ · เซฟไม่สำเร็จ = คืนค่าบิลกลับทุกฟิลด์ (`saveStateOrThrow` + rollback)
> **ค่าใช้จ่ายลงวันตามเวลาที่จ่ายจริง** — `collectExpenses(periodType, periodKey)` ยึด timestamp ของแต่ละรายการ เกณฑ์เดียวกับบิลขาย (เดิมเหมาทั้งกะไปตามวันที่ปิดกะ พอกะลากยาวข้ามตี 6 ค่าใช้จ่ายโดดไปวันถัดไปทั้งก้อน กำไรผิดสองวัน) · ใช้ทั้งสรุปวันและสรุปเดือน · รายการที่ไม่มีเวลาตกไปอยู่วันทำการของกะแทนที่จะหาย · ตารางนับเงินปิดกะยังใช้ยอดรวมทั้งกะเหมือนเดิม (ถูกต้อง — เงินออกจากลิ้นชักของกะนั้นจริง)
> **กะถูกจัดเป็น "คืนของวันที่เปิดร้าน"** — `getClosedShiftsForPeriod` และตารางกะในหน้ารายงานเปลี่ยนจากยึด `endTime` มายึด `startTime` (เดิมคืนที่ปิดช้าเลย 06:00 แถวกะจะไปโผล่ในวันที่ไม่มีบิลขายสักใบ และคืนสิ้นเดือนที่ปิดสายจะกระโดดข้ามไปสรุปเดือนถัดไป) · ร้านเปิด 11:00 เสมอ เวลาเปิดจึงไม่มีทางคาบเกี่ยวเวลาตัดวัน · ผลคือ 3 บล็อกในสรุปวัน (บิลขาย / ค่าใช้จ่าย / ตารางนับเงินปิดกะ) ตรงกันทุกกรณี
> **ส่งออก/สำรองข้อมูลไม่พังเงียบ** — `exportData()` เปลี่ยนจาก data: URL (เพดาน ~2 MB → กดแล้วเงียบเมื่อบิลเยอะ) เป็น Blob + `createObjectURL` คืน true/false ตรวจผลได้ · ก่อนกู้จาก Drive และก่อนนำเข้าไฟล์ ระบบเซฟสำเนาลง IndexedDB (`preRestoreSnapshot`) เสมอ เซฟไม่สำเร็จ = ยกเลิกทั้งหมด · เพิ่มปุ่ม "ย้อนกลับไปก่อนกู้ข้อมูล" ในหน้าตั้งค่า กดสลับไป-กลับได้ · `buildBackupPayload()` เป็นแหล่งเดียวของก้อนข้อมูลสำรองทุกทาง
>
> ✅ **แก้แล้ว (ก.ค. 2569 — audit รอบ 2):** ที่อยู่/เบอร์ร้านบนใบเสร็จเป็นค่าตั้งค่า (เลิก hardcode ข้อมูลปลอม) · void ส่งคำสั่งลบแถวชีตเสมอ + กัน race บิลถูก void ระหว่าง sync (ปิดช่องแถวผี) · กะข้ามเที่ยงคืน/ข้ามเดือนรีเฟรชสรุปทั้งวันเปิดและวันปิดกะ · แก้บิลย้อนหลังรีเฟรชชีตสรุปอัตโนมัติ · clamp ส่วนลด [0, subtotal] · เกลี่ยเศษสตางค์กระจายส่วนลด (largest remainder) · บล็อกขายเมื่อยังไม่มีพนักงาน · PIN ผิด 5 ครั้งล็อก 30 วิ · outbox retry มี backoff 5 นาที · GAS อ่าน master sheet ทั้งคอลัมน์ครั้งเดียว + ปิดการลบไฟล์ backup อัตโนมัติใน Drive · validate shopLogo ตอน import

---

## 📦 IndexedDB State Keys

```
jahn_pos_services         → บริการทั้งหมด
jahn_pos_staff            → พนักงานทั้งหมด
jahn_pos_customers        → ลูกค้าทั้งหมด
jahn_pos_transactions     → ประวัติบิล (ไม่ลบอัตโนมัติ)
jahn_pos_queue            → คิวงานปัจจุบัน
jahn_pos_shift            → ข้อมูลกะ (ไม่ลบอัตโนมัติ)
jahn_pos_shop_owner_pin   → PIN เจ้าของร้าน (SHA-256 hash)
jahn_pos_shop_promptpay   → เลขพร้อมเพย์
jahn_pos_google_sheets_url → URL GAS Web App
jahn_pos_google_sheets_api_token → รหัสเชื่อมต่อ Apps Script (ไม่อยู่ใน Export/Backup)
jahn_pos_telegram_token   → Telegram Bot Token
jahn_pos_telegram_chatid  → Telegram Chat ID
(ระบบเริ่มต้นด้วยพนักงาน/ลูกค้าว่าง — ไม่มีข้อมูล demo ค้างในระบบจริง)
```

---

## 🚀 การ Deploy และ Setup

### 1. Google Apps Script
```
1. เปิด Google Sheets → Extensions → Apps Script
2. วางโค้ดจาก google_apps_script.js
3. รัน `setupPosApiToken` 1 ครั้ง แล้วคัดลอกรหัสจาก Execution log
4. Deploy → New deployment → Web app
   - Execute as: Me
   - Who has access: Anyone
5. คัดลอก URL + รหัสเชื่อมต่อ → ใส่ในหน้าตั้งค่า POS ทุกเครื่อง
```

### 2. เปิดใช้งาน POS
```
Option A: เปิด index.html ตรงๆ ใน Chrome (ง่ายสุด)
Option B: Local server → python -m http.server 8080
Option C: Host บน GitHub Pages / Netlify (PWA เต็มรูปแบบ)
```

### 3. ติดตั้งบน Mobile (PWA)
```
Android: Chrome → เมนู ⋮ → Add to Home Screen
iOS:     Safari → Share → Add to Home Screen
```

---

## 🔢 Version History (สรุป)

| Feature | รายละเอียด |
|---------|-----------|
| Core POS | ขาย, คิว, ลูกค้า, พนักงาน, บริการ |
| Security | SHA-256 PIN, Role-based access |
| Cloud Sync | Google Sheets (transaction + summary) |
| Notification | Telegram Bot (shift-close report) |
| UI | Dark Premium, PWA, Mobile-first |
| Validation | Phone format, duplicate check, price/duration |
| Reliability | ป้องกันบันทึกล้มเหลว, ไม่ลบประวัติอัตโนมัติ, lazy render |

---

## ⚠️ ข้อจำกัดเชิงสถาปัตยกรรม (ต้องรู้ก่อนใช้/ส่งต่อ)

1. **ใช้เครื่องเดียวเท่านั้น (single-device)** — ข้อมูลเก็บใน IndexedDB ของแต่ละเครื่องแยกกัน ถ้าเปิดขายพร้อมกัน 2 เครื่อง สรุปวัน/เดือนบนชีตจะมาจากเครื่องที่กดปิดกะเท่านั้น (บิลของอีกเครื่องไม่ถูกรวม) ห้ามใช้หลายเครื่องพร้อมกัน
2. **รหัสเชื่อมต่อช่วยปิดช่องใน GitHub แต่ไม่ใช่ระบบล็อกอินเต็มรูปแบบ** — รหัสอยู่ใน Script Properties และ IndexedDB ของ iPad ไม่อยู่ใน source/Export; แต่คนที่เข้าถึง iPad ที่ปลดล็อกอาจดึงรหัสได้ ถ้าสงสัยว่ารั่ว ให้รัน `rotatePosApiToken` แล้วเปลี่ยนรหัสในทุกเครื่อง สำหรับหลายสาขาหรือผู้ใช้จำนวนมาก ควรย้ายไป backend ที่มีบัญชีผู้ใช้จริง
3. **Timezone ของ Apps Script ควรตั้งเป็น Bangkok** — ตั้งแต่ v1.2.0 แอปส่ง monthKey จากเวลาหน้าร้านไปด้วย บิลจึงลงแท็บถูกเดือนแม้ timezone ฝั่ง GAS ผิด แต่ timestamp "สร้างเมื่อ" และรอบลบแท็บรายวันเก่ายังอิง timezone ของโปรเจกต์ GAS (Project Settings > Time zone)
4. **PIN ป้องกันเฉพาะระดับหน้าจอ** — คนที่เข้าถึง DevTools ของเครื่องร้านได้สามารถอ่านข้อมูล/เดา PIN แบบ offline ได้ (PIN 6 หลัก + SHA-256) เหมาะกับ threat model ร้านค้า ไม่ใช่ระบบความปลอดภัยระดับธนาคาร
5. **ระบบนับยอดแบบ "วันทำการ" ตัดวันตอน 06:00 น.** (ตั้งแต่ v1.3.0) — ร้านเปิด 11:00 ถึงตี 3: บิล/ค่าใช้จ่ายก่อน 06:00 เช้า นับเป็นยอดของ "เมื่อวาน" ทั้งหมด (แดชบอร์ด, สรุปวัน/เดือนบนชีต, รายงาน, กราф) ผลข้างเคียงที่ต้องรู้ตอนทำบัญชี: บิลช่วง 00:00–06:00 ของเช้าวันที่ 1 จะลงแท็บเดือนของเดือนก่อนหน้า (ยึดคืนสิ้นเดือน) และการแก้/void บิลช่วงตี 0–6 ที่ sync ไว้ตั้งแต่ก่อนอัปเดต v1.3.0 อาจชี้แท็บเดือนไม่ตรงแถวเดิม (ต้องลบแถวเก่าในชีตเอง — เกิดเฉพาะบิลเก่าคาบเกี่ยวเที่ยงคืน) ค่าตัดวันแก้ได้ที่ค่าคงที่ `BUSINESS_DAY_CUTOFF_HOUR` ใน app.js

## 🧪 สถานะการทดสอบ (26 ส.ค. 2569)

| วัดอะไร | ผล |
|---|---|
| ข้อทดสอบตรรกะ (`node tests/run-all.js`) | **685 ข้อ ผ่านหมด** |
| ข้อทดสอบหน้าจอแบบนับผ่าน/ไม่ผ่าน (`uiguard` + `e2e_*`) | **118 ข้อ ผ่านหมด** (ต้องลง Playwright ก่อน) |
| ชุดหน้าจอแบบรายงานตาราง (`responsive`/`modaltest`/`browsertest`/`offlinetest`) | ไม่มีคอลัมน์ล้นจอ · ทุกป๊อปอัปกดปุ่มถึง · ออฟไลน์ฟอนต์+ไอคอนยังขึ้น |
| เมธอดใน `app.js` ที่ถูกเรียกจริงอย่างน้อย 1 ครั้ง | **185/185** |
| ฟังก์ชันใน `google_apps_script.js` ที่ถูกเรียกจริง | **42/42** |
| จงใจทำโค้ดพังแล้วดูว่าเทสต์จับได้ไหม (mutation) | **จับได้ 16/16 แบบ** |

> **อ่านตัวเลขนี้ให้ถูก:** "185/185" แปลว่าไม่มีฟังก์ชันไหนที่ไม่เคยถูกเรียกเลย
> ไม่ได้แปลว่าทุก if/else ข้างในถูกตรวจ และไม่ได้แปลว่าไม่มีบั๊กเหลือ
> ส่วนที่ยัง**ทดสอบอัตโนมัติไม่ได้**: Apps Script ตัวจริงบน Google (เทสต์ใช้ตัวจำลอง),
> เครื่องพิมพ์สลิปจริง, การสแกน QR ด้วยแอปธนาคารจริง, และพฤติกรรมบน iPad/Safari จริง

---

*เอกสารนี้สร้างอัตโนมัติจากการวิเคราะห์โค้ด — อัปเดตล่าสุด: 26 สิงหาคม 2569 (v1.5.6)*
