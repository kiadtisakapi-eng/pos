# ไฟล์สำรอง/ขยะ ไม่ต้องขึ้น GitHub
backup/
erotica_pos_backup_*.json
pos_backup_*.json
*.backup.json
.env
.env.*
*.secret
__pycache__/
*.pyc
refactor.py
_backup_final_qa_*.js

# local repair scripts (เก็บไว้ในเครื่อง ไม่ต้องขึ้น repo)
deploy.bat
first-setup.bat
fix-push.bat
fix-push.ps1

# ไฟล์ขยะจาก Windows/macOS (deploy.ps1 ใช้ git add -A จะกวาดติดไปด้วยถ้าไม่กัน)
desktop.ini
Thumbs.db
.DS_Store

# ไฟล์ที่งอกมาตอนรันเทสต์หน้าจอ (node tests/run-all-ui.js)
# ไม่กันไว้ = git status ไม่มีวันว่าง = deploy.ps1 เห็นว่า "มีของเปลี่ยน" ทุกครั้ง
# แล้วบวกเลขแคช + push เปล่าทั้งที่โค้ดเหมือนเดิม (iPad จะขึ้นว่ามีอัปเดตฟรี ๆ)
node_modules/
shots/
responsive-result.json

# รายงานผลตรวจ — เป็นแผนที่ช่องโหว่ของระบบที่จับเงินจริง
# repo นี้เป็น public + เสิร์ฟผ่าน GitHub Pages ใครก็เปิดอ่านได้
# เก็บไว้ในเครื่องอย่างเดียว ห้ามขึ้น repo
รายงานตรวจโปรเจกต์_*.md
รายงานปิดโปรเจกต์_*.md
