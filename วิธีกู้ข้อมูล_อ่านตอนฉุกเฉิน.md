# กู้ข้อมูลตอน iPad หาย / เครื่องพัง

> อ่านหน้านี้หน้าเดียวพอ ไม่ต้องเปิดไฟล์อื่น

---

## ระบบสำรองข้อมูลให้อัตโนมัติอยู่แล้ว

ทุกครั้งที่ **ปิดกะ** แอปจะส่งข้อมูลทั้งร้านขึ้น Google Drive เป็นไฟล์
เก็บไว้ในโฟลเดอร์ชื่อ `Erotica_POS_Backups` **เก็บย้อนหลัง 90 วัน** ไฟล์ที่เก่ากว่านั้นถูกลบอัตโนมัติ

แปลว่า: ข้อมูลที่จะหายจริง ๆ คือ **เฉพาะกะที่ยังไม่ได้ปิด** เท่านั้น
เพราะไฟล์สำรองแต่ละไฟล์เก็บบิล *ทั้งหมดตั้งแต่เปิดร้าน* ไม่ใช่เฉพาะกะนั้น
ไฟล์ล่าสุดเพียงไฟล์เดียวจึงกู้ได้ครบทั้งร้าน — ไม่ต้องไปตามหาไฟล์เก่า

> หน้ากู้ข้อมูลในแอปแสดงไฟล์ล่าสุด 50 ไฟล์ ปกติใช้ไฟล์บนสุดเสมอ
> ถ้าจำเป็นต้องย้อนไกลกว่านั้น เปิดโฟลเดอร์ `Erotica_POS_Backups` ใน Google Drive ตรง ๆ ได้

---

## ขั้นตอนกู้ (5 นาที)

1. เปิดแอปบนเครื่องใหม่ → ล็อกอินด้วย PIN เจ้าของ
   (เครื่องใหม่ PIN จะเป็นค่าเริ่มต้น `123456`)

   > ⏱️ **ตั้งแต่ v1.5.2:** สิทธิ์เจ้าของร้านหมดอายุเองเมื่อไม่แตะจอครบ 5 นาที
   > ระหว่างทำขั้นตอนข้างล่างถ้าหยุดคิดนาน อาจเด้งหน้าใส่ PIN ใหม่ — ใส่แล้วทำต่อได้เลย ไม่มีอะไรหาย
2. ไปหน้า **ตั้งค่า**
3. วาง **Google Sheets URL** และ **รหัสเชื่อมต่อ Apps Script** ตัวเดิม → กดบันทึก
   ⚠️ ขั้นนี้ข้ามไม่ได้ ถ้าใส่ไม่ครบ ปุ่มกู้ข้อมูลจะกดไม่ได้
4. เลื่อนลงไปที่การ์ด **จัดการความปลอดภัยและข้อมูลระบบ**
5. กดปุ่ม **กู้ข้อมูลจาก Google Drive**
6. เลือกไฟล์ที่ต้องการ — **ไฟล์บนสุดคือไฟล์ใหม่สุด** (มีป้าย "ล่าสุด")
7. กดยืนยัน → รอสักครู่ → เสร็จ

---

## สิ่งที่ต้องตั้งใหม่เองหลังกู้ (ระบบจงใจไม่เก็บไว้ในไฟล์สำรอง)

| อะไร | ทำไม | ทำยังไง |
|---|---|---|
| **PIN เจ้าของร้าน** | ถ้าเก็บไว้ในไฟล์ ใครได้ไฟล์ไปก็เข้าระบบได้ | ตั้งค่า → เปลี่ยน PIN |
| **รหัสเชื่อมต่อ Apps Script** | รหัสไม่อยู่ในไฟล์สำรอง เพื่อไม่ให้ไฟล์กู้ข้อมูลใช้เข้าถึง Drive ได้ | ตั้งค่า → วางรหัสเดิม; ถ้าหาไม่เจอให้เปิด Apps Script แล้วรัน `setupPosApiToken` เพื่อดูรหัสเดิม |
| **Telegram Token** | เป็นกุญแจส่งข้อความ หลุดแล้วอันตราย | ตั้งค่า → วาง Token ใหม่ |

---

## ข้อควรระวัง

- **ปุ่มนี้เขียนทับข้อมูลในเครื่องทั้งหมด** ถ้ากดบนเครื่องที่กำลังขายอยู่ ยอดของวันนี้จะหาย

  **แต่กดผิดแล้วย้อนกลับได้ทันที** — ก่อนกู้ทุกครั้ง ระบบจะเซฟสำเนาของ "ตอนนี้" ไว้ในเครื่องให้ก่อนเสมอ
  (เซฟไม่สำเร็จ = ยกเลิกการกู้ทั้งหมด ไม่ยอมเขียนทับ)

  > 🔙 **วิธีย้อนกลับ:** ตั้งค่า → การ์ด *จัดการความปลอดภัยและข้อมูลระบบ* → ปุ่ม **"ย้อนกลับไปก่อนกู้ข้อมูล"**
  > ปุ่มนี้โผล่เฉพาะเมื่อมีสำเนาอยู่จริง และบอกด้วยว่าสำเนานั้นเซฟไว้เมื่อไหร่ มีกี่บิล
  > กดแล้วสลับไป-กลับได้ เพราะก่อนย้อนมันจะเก็บสถานะปัจจุบันไว้แทนที่ให้อีกที

  ระบบยังพยายามโหลดไฟล์ `erotica_pos_backup_วันที่.json` ติดเครื่องไว้ให้อีกชั้นด้วย
  (ใช้กับปุ่ม **นำเข้าข้อมูลสำรอง**) แต่บน iPad การดาวน์โหลดอาจถูกบล็อกเงียบ ๆ — **ให้ยึดปุ่มย้อนกลับเป็นหลัก**

- **ถ้าไฟล์สำรองมีข้อมูลเสียบางส่วน** ระบบจะขึ้นกล่องเตือนบอกก่อนว่าเสียกี่บิล เสียแบบไหน
  (ยอดเงินหาย / วันที่ใช้ไม่ได้ / ไม่มีเลขที่บิล / เลขซ้ำ) แล้วให้เลือกว่าจะกู้ต่อหรือลองไฟล์อื่น
  ถ้ากู้ต่อ ส่วนที่ดีจะเข้าครบ ส่วนที่เสียจะถูกนับเป็น 0 บาท (ไม่ทำให้ยอดทั้งวันพังตามไปด้วย)
- ปุ่มนี้ **เจ้าของร้านเท่านั้น** ที่เห็นและกดได้
- ถ้าแอปขึ้น **จอแดง "โหลดข้อมูลร้านไม่สำเร็จ"** ห้ามกู้ทับ ให้ปิดแอปเปิดใหม่ก่อน
  (ข้อมูลจริงน่าจะยังอยู่ครบ แค่อ่านไม่ออกชั่วคราว)

---

## ถ้าปุ่มกู้ข้อมูลใช้ไม่ได้ (ทางสำรอง)

1. เปิด Google Drive ด้วยบัญชีเดียวกับที่ทำชีต
2. เข้าโฟลเดอร์ `Erotica_POS_Backups`
3. ดาวน์โหลดไฟล์ `pos_backup_ปี-เดือน-วัน_เวลา.json` ที่ใหม่สุด
4. ในแอป → ตั้งค่า → **นำเข้าข้อมูลสำรอง (.json)** → เลือกไฟล์นั้น
