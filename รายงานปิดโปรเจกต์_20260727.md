# รายงานปิดโปรเจกต์ — Erotica POS
**วันที่ตรวจ:** 27 กรกฎาคม 2569
**ขอบเขต:** `app.js` (4,745 บรรทัด) · `google_apps_script.js` · `index.html` · `style_v2.css` · `sw.js`

---

## บทสรุปผู้บริหาร

ระบบนี้**ใช้งานได้จริงและออกแบบมาดีกว่ามาตรฐานของงานขนาดนี้** มีการกัน race condition, idempotency, offline-first ที่ถูกต้อง ซึ่งงาน POS ระดับร้านเล็กส่วนใหญ่ไม่มี

แต่มี **บั๊ก 1 ตัวที่ทำข้อมูลหายทั้งร้านได้** และควรแก้ก่อนใช้งานต่อระยะยาว

| ระดับ | จำนวน | สรุป |
|---|---|---|
| 🔴 วิกฤต | 1 | ข้อมูลหายถาวรได้ |
| 🟠 สูง | 3 | ตัวเลขไม่ตรง / กู้ข้อมูลไม่ได้ |
| 🟡 กลาง | 6 | ประสิทธิภาพ + ความสะดวก |
| 🔵 ต่ำ | 7 | ข้อสังเกต ไม่ต้องรีบ |
| ✅ ทำถูกแล้ว | 12 | ห้ามแก้ |

---

# ส่วนที่ 1 — ปัญหาที่ต้องแก้

## 🔴 [C-1] โหลดฐานข้อมูลพลาดครั้งเดียว = ข้อมูลหายถาวร

**ไฟล์:** `app.js:525-548`

**เกิดอะไรขึ้น**
ตอนเปิดแอป ถ้าอ่าน IndexedDB ไม่สำเร็จ โค้ดใน `catch` จะรีเซ็ต state เป็นค่าว่างทั้งหมด:

```js
} catch (err) {
  this.state.transactions = [...DEFAULT_TRANSACTIONS];  // = []
  this.state.staff        = [...DEFAULT_STAFF];         // = []
  this.state.customers    = [...DEFAULT_CUSTOMERS];     // = []
  this.state.shift        = { ... history: [] };        // ประวัติกะหายหมด
  this.ownerPin           = await this.hashPin('123456');
  // ← ไม่มีอะไรกัน saveState() ไม่ให้ทำงานต่อ
}
```

แอปจะเปิดขึ้นมาเหมือนติดตั้งใหม่ แล้ว**พอพนักงานกดอะไรก็ตาม** (เปิดกะ ออกบิล แก้ชื่อ — รวม 32 จุดที่เรียก `saveState()`) ข้อมูลว่างจะถูกเขียนทับของจริงทันที

มีแค่ toast เตือน 6 วินาที ซึ่งพนักงานจะกดปิดแล้วขายต่อ

**เกิดขึ้นจริงได้ตอนไหน**
- เปิดแอปค้างไว้ 2 แท็บ/2 หน้าต่าง แล้วอัปเดตเวอร์ชัน → IndexedDB upgrade ถูกบล็อก
- iOS ล้าง storage ตอนเครื่องความจำเต็ม
- iPad แบตหมด/ดับกลางการเขียน → DB corrupt
- Safari private mode หรือ storage ถูกปิด

**ความมั่นใจ:** โค้ดเป็นแบบนี้แน่นอน (อ่านจากไฟล์โดยตรง) · **ความถี่ที่จะเกิด: ประเมินไม่ได้** ต้องดูจากหน้างานว่าเคยมีอาการ "ข้อมูลหายเฉย ๆ" ไหม

**วิธีแก้ (~15 บรรทัด)**
```js
// ใน catch ของ loadState()
this.loadFailed = true;

// ต้นฟังก์ชัน saveState()
if (this.loadFailed) {
  console.error('[GUARD] ปฏิเสธการบันทึก — โหลดข้อมูลไม่สำเร็จ');
  return;
}
```
พร้อมขึ้นจอทึบเต็มหน้า "โหลดข้อมูลไม่สำเร็จ — ห้ามขาย ปิดแอปแล้วเปิดใหม่" ที่ปิดไม่ได้

**ทำไมต้องบล็อกทั้งหมด ไม่ใช่แค่เตือน** — เพราะการขายต่อในสถานะนี้ทำลายข้อมูลเก่าไปเรื่อย ๆ การหยุดขาย 5 นาทีเสียหายน้อยกว่าเสียประวัติทั้งร้าน

---

## 🟠 [H-1] แถวผีในชีต — บิลที่ยกเลิกแล้วลบไม่ออก

**ไฟล์:** `app.js:697` (`clearSalesData`)

```js
this.state.cloudOutbox = []; // ล้างงานคลาวด์ที่ค้างทั้งหมด
```

ถ้าตอนกดปุ่ม "ล้างยอดขาย" มีคำสั่ง**ลบแถวบิล**ค้างอยู่ที่ยังส่งไม่สำเร็จ (เน็ตหลุด / GAS ล่ม) คำสั่งลบจะหายไปด้วย → **แถวบิลที่ยกเลิกค้างในชีตถาวร ไม่มีอะไรมาลบให้อีก**

ผลคือแท็บบิลรายเดือนมียอดสูงกว่าชีตสรุป โดยไม่มีร่องรอยว่าเกิดจากอะไร

**วิธีแก้ (~4 บรรทัด)**
```js
// เก็บเฉพาะคำสั่งลบแถว ตัดงานสรุปทิ้ง (งานสรุปเก่าจะ flush ยอดศูนย์ไปทับชีตวันเก่า)
this.state.cloudOutbox = this.state.cloudOutbox
  .filter(it => it.needVoidDelete)
  .map(it => ({ ...it, needSummary: false, needTelegram: false }));
```

---

## 🟠 [H-2] `resetData()` ลบทุกอย่างด้วยการยืนยันครั้งเดียว

**ไฟล์:** `app.js:679-691`

```js
await db.state.clear();
localStorage.clear();
```

กดพลาดครั้งเดียว = ข้อมูลทั้งร้านหาย **กู้ไม่ได้** ปุ่มนี้อยู่ในหน้าตั้งค่าเดียวกับปุ่มอื่นที่ใช้ประจำ

**วิธีแก้** บังคับพิมพ์คำยืนยัน (เช่นพิมพ์ `ลบทั้งหมด`) + สั่ง `autoBackupToGoogleDrive()` ให้สำเร็จก่อนถึงจะยอมลบ

---

## 🟠 [H-3] กู้ข้อมูลจาก Drive ต้องทำมือทั้งหมด

แอปส่ง backup ขึ้น Drive อัตโนมัติทุกครั้งที่ปิดกะ (ดีมาก) แต่**ไม่มีปุ่มดึงกลับ** ต้อง:
1. เปิด Google Drive หาโฟลเดอร์ `Erotica_POS_Backups`
2. ดาวน์โหลดไฟล์ `pos_backup_*.json` ที่ถูกต้อง
3. เอาเข้าแอปผ่านปุ่ม import

ถ้า iPad หายตอนตี 2 แล้วต้องเปิดร้านพรุ่งนี้ ขั้นตอนนี้ต้องมีคนทำเป็น

**สิ่งที่จะไม่ได้คืนแม้ restore สำเร็จ:**
- `ownerPin` — จงใจตัดออกเพื่อความปลอดภัย → กลับเป็น `123456`
- `telegramToken` — จงใจตัดออก → ต้องตั้งใหม่

**ควรทำ:** เขียนขั้นตอนกู้ข้อมูลให้คนที่ไม่ใช่คุณทำตามได้
→ ✅ ทำแล้ว อยู่ใน `วิธีกู้ข้อมูล_อ่านตอนฉุกเฉิน.md` (เดิมชี้ไป `คู่มือส่งต่อ_วิธีอัปเดตแอป.md` ซึ่งถูกลบเมื่อ 2026-08-05 เพราะเนื้อหาค้างอยู่ที่สถานะเดือน มิ.ย.)

---

## 🟡 [M-1] `archiveOldData()` วิ่งทุกครั้งที่บันทึก

**ไฟล์:** `app.js:588`

`saveState()` เรียก `archiveOldData()` เป็นบรรทัดแรกเสมอ ซึ่งวนกรอง array บิลทั้งปี + ประวัติกะ + voidLog ทุกครั้ง ทั้งที่งานนี้จำเป็นแค่วันละครั้ง

**แก้:** ย้ายไปเรียกใน `init()` และตอนปิดกะเท่านั้น

---

## 🟡 [M-2] เขียนทับข้อมูลทั้งก้อนทุกครั้งที่บันทึก

**ไฟล์:** `app.js:590-613`

`db.state.bulkPut([{ key:'transactions', value: [...บิลทั้งหมด] }])` — บิลทั้งปีเก็บเป็น **แถวเดียว** ใน IndexedDB ทุกครั้งที่บันทึกคือเขียนทับทั้งก้อน

| ปริมาณข้อมูล | ขนาดต่อการเขียน 1 ครั้ง |
|---|---|
| 1 เดือน (450 บิล) | 0.35 MB |
| 1 ปี (5,475 บิล) | 4.3 MB |

ขายวันละ 15 บิล × บันทึกราว 3 ครั้ง/บิล = **เขียนดิสก์ ~190 MB/วัน** เมื่อข้อมูลครบปี

**แก้ (งานใหญ่ ไว้ทีหลัง):** แยกบิลเป็นตารางของตัวเองใน Dexie
```js
db.version(3).stores({ state: 'key', transactions: 'id, date' });
```
แล้วเขียนเฉพาะบิลที่เปลี่ยน

---

## 🟡 [M-3] วาดหน้าจอใหม่ทั้ง 6 หน้าทุกครั้ง

**ไฟล์:** `app.js:3195` (`renderAll`)

```js
renderAll() {
  this.renderDashboard(); this.renderPos(); this.renderQueueScreen();
  this.renderCustomerTable(); this.renderReports(); this.renderSettingsLists();
}
```

ตัวหนักคือ `renderReports()` → `filterReports()` ที่**วนอ่านบิลทั้งปีแล้วแตกรายการบริการทุกใบออกมาเรียงใหม่** — จบบิล 1 ใบ = ทำงานเท่ากับเปิดหน้ารายงานเต็ม ๆ ทั้งที่พนักงานยังอยู่หน้าขาย

**แก้ (~5 บรรทัด):** วาดเฉพาะหน้าที่เปิดอยู่ตาม `this.state.activeScreen`

**เมื่อไหร่จะรู้สึก:** ประเมินว่าราวหลักพันบิลขึ้นไป — **ยังไม่ได้วัดจริงบน iPad ที่ใช้อยู่**

---

## 🟡 [M-4] ปุ่มเล็กเกินขนาดนิ้ว

**ไฟล์:** `style_v2.css:1211` (`.btn-icon`) และ `1179` (`.btn-small`)

| องค์ประกอบ | ขนาดจริง | เกณฑ์ Apple HIG / WCAG |
|---|---|---|
| ปุ่มไอคอน (ลบ/แก้ไข) | ~30 × 30 px | 44 × 44 px |
| ปุ่มเล็ก | ~29 px สูง | 44 px |

ปุ่ม**ลบ**อยู่ติดปุ่ม**แก้ไข** ขนาดเท่ากัน พนักงานมือเปียกหรือรีบตอนลูกค้ารอ มีโอกาสกดผิด

**แก้ (1 บรรทัด):**
```css
.btn-icon { min-width: 44px; min-height: 44px; }
```
พื้นที่กดขยายออกด้านนอก หน้าตาแทบไม่เปลี่ยน

---

## 🟡 [M-5] ตัวหนังสือเล็กสุด ~10 px

`style_v2.css` มี `font-size: 0.65rem` (≈10.4 px) ในป้ายกำกับหลายจุด กะดึกแสงร้านสลัวจะอ่านลำบาก โดยเฉพาะพนักงานอายุมาก

---

## 🟡 [M-6] ประวัติกะถูกลบที่ 90 วัน

**ไฟล์:** `app.js:559-567`

```js
this.state.shift.history = this.state.shift.history.filter(sh =>
  (now - ts) < ninetyDaysMs);
```

หลังแก้เรื่องคอลัมน์เงินขาด/เกินแล้ว ตัวเลขสะสมยังอยู่บน master sheet ถาวร **จึงไม่ใช่เรื่องเร่งด่วนแล้ว** แต่ผลข้างเคียงคือ: re-sync สรุปย้อนหลังเกิน 90 วันจะได้ค่าว่าง

---

# ส่วนที่ 2 — ช่องโหว่ความปลอดภัย

## 🔵 [S-1] `API_SECRET` ฝังอยู่ในไฟล์ที่ deploy ขึ้นเว็บ

**ไฟล์:** `app.js:36`
```js
const API_SECRET = '[REDACTED — rotated and removed from source]';
```

ใครเปิด view-source ก็เห็น → ยิงข้อมูลปลอมเข้าชีตได้ หรือเรียก `action:'backup'` เพื่อดูดข้อมูลร้านทั้งหมด

**นี่คือข้อจำกัดของสถาปัตยกรรมที่ไม่มี backend — แก้ให้หายขาดไม่ได้** ถ้าจะแก้จริงต้องมีเซิร์ฟเวอร์กลางถือ secret ซึ่งเกินขอบเขตโปรเจกต์

**ลดความเสี่ยงได้:**
- อย่า deploy เป็น public URL ที่ค้นเจอใน Google (ใช้ repo private / URL เดาไม่ได้)
- เปลี่ยน `API_SECRET` เป็นระยะ (ต้องเปลี่ยนทั้ง 2 ไฟล์พร้อมกัน)
- ตั้ง GAS ให้ deploy แบบจำกัดสิทธิ์เท่าที่จำเป็น

## 🔵 [S-2] PIN แฮชแบบ SHA-256 ไม่มี salt

**ไฟล์:** `app.js:4198` (`hashPin`)
```js
const data = encoder.encode('jahn_pos_v2_' + pin);
await crypto.subtle.digest('SHA-256', data);
```

PIN 6 หลัก = 1 ล้านความเป็นไปได้ ใครได้ค่าแฮชไปไล่หาเจอในไม่กี่วินาที (prefix `jahn_pos_v2_` เป็นค่าคงที่ ไม่ใช่ salt จริง)

**ความเสี่ยงจริงต่ำ** เพราะแฮชอยู่ใน IndexedDB ในเครื่อง — คนที่เข้าถึงได้ก็เข้าถึงข้อมูลตรง ๆ ได้อยู่แล้ว ไม่ต้องถอด PIN

## 🔵 [S-3] Telegram token เก็บเป็นข้อความธรรมดา

อยู่ใน IndexedDB แบบ plaintext ใครเปิด DevTools บน iPad ที่ปลดล็อกได้ก็ copy ไปใช้ส่งข้อความปลอมในนามร้านได้ — **จงใจตัดออกจาก backup แล้ว ซึ่งถูกต้อง**

## 🔵 [S-4] การฉีดโค้ดผ่านไฟล์ import

**ไฟล์:** `app.js` (21 จุดที่ใช้ `onclick="app.xxx('${id}')"`)

id ถูกแทรกลงใน HTML โดยตรง ถ้ามีคนส่งไฟล์ backup ที่จงใจใส่ id แปลก ๆ มาให้ import จะรันโค้ดได้

**ความเสี่ยงต่ำมาก** — ต้องเป็นไฟล์ที่คนอื่นส่งมา ถ้า import แค่ไฟล์ที่ระบบตัวเองสร้าง ความเสี่ยงเป็นศูนย์

**กฎง่าย ๆ: อย่า import ไฟล์ JSON ที่ไม่ได้มาจาก Drive ของตัวเอง**

## 🔵 [S-5] `postVoidDelete` กลืน error จริงบางกรณี

**ไฟล์:** `app.js:4681`
```js
if (d.status === 'error' && /ไม่พบ/.test(d.message)) return true;
```

ข้อความ `"ไม่พบแผ่นงานของเดือนนี้"` ก็ผ่านเงื่อนไขนี้ → ถ้าชีตเดือนนั้นหายไปจริง ๆ ระบบจะคิดว่า "ลบสำเร็จ" ทั้งที่ยังไม่ได้ลบ

**ความเสี่ยงต่ำ** เพราะถ้าไม่มีชีตเดือนนั้นแปลว่าบิลไม่เคยขึ้นชีตอยู่แล้ว แต่ถ้าอยากเนี้ยบให้แยกเช็คเฉพาะ `/ไม่พบบิลเลขที่/`

---

# ส่วนที่ 3 — ปัญหาที่จะเกิดในอนาคต

## 📈 [F-1] ข้อมูลโตจนแอปหน่วง — คาดว่าเดือนที่ 8-12

รวมผลของ [M-1] + [M-2] + [M-3] เข้าด้วยกัน อาการที่จะเห็นเรียงตามลำดับ:
1. กดจบบิลแล้วรอ 1 วินาทีก่อนหน้าจอเปลี่ยน
2. เปิดหน้ารายงานรายเดือนแล้วค้าง 2-3 วินาที
3. เปิดแอปครั้งแรกของวันช้าลงชัดเจน

**ตัวชี้วัดให้เฝ้าดู:** ถ้าเริ่มรู้สึกหน่วงตอนจบบิล ให้แก้ [M-3] ก่อน (5 บรรทัด ได้ผลทันที) ถ้ายังไม่พอค่อยทำ [M-2]

## 📈 [F-2] Google Drive backup กินพื้นที่

ทุกครั้งที่ปิดกะสร้างไฟล์ backup ใหม่ เก็บ 30 วัน

| ปริมาณข้อมูล | ขนาดไฟล์ | 60 ไฟล์ (30 วัน × 2 กะ) |
|---|---|---|
| 1 เดือน | 0.7 MB | 40 MB |
| 6 เดือน | 4.0 MB | 240 MB |
| 1 ปี | 8.2 MB | **480 MB** |

Google Drive ฟรี 15 GB ยังไหว แต่ถ้าบัญชีนั้นใช้เก็บอย่างอื่นด้วยควรเฝ้าดู ปรับได้ที่ `BACKUP_RETENTION_DAYS` ใน `google_apps_script.js` (ค่าปัจจุบัน 90 วัน)

> **หมายเหตุ (2026-08-05):** เคยมีการตั้งค่านี้เป็น `0` (ไม่ลบเลย) โดยให้เหตุผลว่าปลอดภัยกว่าตอนกู้ข้อมูล — เปลี่ยนกลับเป็น 90 แล้ว
> เหตุผล: ไฟล์สำรองแต่ละไฟล์เก็บบิล *ทั้งหมดตั้งแต่เปิดร้าน* ไม่ใช่เฉพาะกะนั้น พื้นที่จึงโตแบบกำลังสอง ไม่ใช่เชิงเส้นตามตารางด้านบน
> พอ Drive เต็ม การสำรองจะล้มเหลวแบบเงียบ ๆ ซึ่งเสี่ยงกว่าการไม่มีไฟล์เก่ากว่า 90 วันให้ย้อนดูมาก

## 📈 [F-3] จำนวนชีตสะสมใน Spreadsheet

| | ปีที่ 1 | ปีที่ 3 |
|---|---|---|
| ชีตสรุปรายวัน (ลบที่ 62 วัน) | 62 | 62 |
| แท็บบิลรายเดือน (**ไม่มีการลบเลย**) | 12 | 36 |
| ชีตสรุปรายเดือน master | 1 | 1 |
| **รวม** | ~75 | ~99 |

Google Sheets เปิดช้าลงเมื่อชีตเยอะ ปีที่ 3-4 ควรย้ายแท็บเก่าไปไฟล์แยก **ยังไม่ต้องทำตอนนี้**

## ⚠️ [F-4] ถ้าเปลี่ยนเวลาตัดวันทำการ ข้อมูลเก่าจะเพี้ยน

**ไฟล์:** `app.js:47` — `BUSINESS_DAY_CUTOFF_HOUR = 6`

ค่านี้ใช้คำนวณว่าบิลใบไหนอยู่วันทำการไหน ถ้าวันหนึ่งเปลี่ยนเวลาเปิด-ปิดร้านแล้วแก้ค่านี้:
- บิลช่วงคาบเกี่ยวจะย้ายวัน/เดือน
- re-sync จะสร้างแถวใหม่ในแท็บเดือนอื่น → **บิลเดิมโผล่ 2 ที่**
- สรุปย้อนหลังทั้งหมดเปลี่ยนตัวเลข

**ถ้าจำเป็นต้องเปลี่ยน ให้เปลี่ยนต้นเดือนใหม่เท่านั้น และห้าม re-sync ข้อมูลเก่า**

## ⚠️ [F-5] iPad เครื่องเดียวคือจุดตายจุดเดียว

ทุกอย่างอยู่ในเครื่องเดียว ระบบไม่รองรับหลายเครื่องพร้อมกัน (ไม่มีการ merge — เครื่องที่บันทึกทีหลังทับข้อมูลเครื่องแรก)

**ถ้าอนาคตอยากใช้ 2 เครื่อง** ต้องรื้อสถาปัตยกรรมทั้งหมด ไม่ใช่แค่เพิ่มฟีเจอร์ ประเมินคร่าว ๆ ว่าเป็นการเขียนใหม่มากกว่าครึ่งระบบ

## ⚠️ [F-6] เพดานโควตา Google Apps Script

บัญชี Gmail ทั่วไปมีเพดานการเรียก URL Fetch ต่อวัน ปัจจุบันใช้ไม่ถึง (บิล 15 ใบ + สรุป + backup ≈ 20 ครั้ง/วัน) **ยังห่างเพดานมาก** แต่ถ้าเพิ่มร้าน/เพิ่มการซิงก์ถี่ขึ้นควรเช็คก่อน

**ตัวเลขเพดานเปลี่ยนได้ ต้องไปดูเอกสาร Google ตอนที่จะขยาย — ผมไม่ยืนยันตัวเลขปัจจุบัน**

## ⚠️ [F-7] Font Awesome โหลดจาก CDN

`index.html` ดึงไอคอนจาก `cdnjs.cloudflare.com` ถ้าติดตั้งแอปบน iPad เครื่องใหม่**ตอนไม่มีเน็ต** ไอคอนทั้งแอปจะกลายเป็นสี่เหลี่ยม (service worker แคชแบบ dynamic ไว้แล้ว แต่ต้องโหลดสำเร็จอย่างน้อย 1 ครั้งก่อน)

**ป้องกัน:** ตอนตั้งเครื่องใหม่ ให้เปิดแอปตอนมีเน็ตอย่างน้อย 1 รอบก่อนเอาไปใช้หน้าร้าน

## ⚠️ [F-8] `app.js` 250 KB ในคลาสเดียว

4,745 บรรทัด รวมทุกอย่างตั้งแต่วาดหน้าจอ คำนวณเงิน ยัน sync คลาวด์

**นี่ไม่ใช่ข้อผิดพลาด** — ไม่มี build step แปลว่าแก้แล้ว deploy ได้เลย เป็น trade-off ที่สมเหตุสมผลสำหรับคนดูแลคนเดียว

**ความเสี่ยงจริงคือ:** ถ้าวันหนึ่งคุณไม่ได้ดูแลเอง คนใหม่จะใช้เวลานานมากกว่าจะกล้าแก้ ควรมี `SYSTEM_OVERVIEW.md` ที่อัปเดตตลอด (มีแล้ว — รักษาไว้)

---

# ส่วนที่ 4 — สิ่งที่ทำถูกแล้ว ห้ามแก้

ส่วนนี้สำคัญพอ ๆ กับรายการปัญหา เพราะเป็นจุดที่คนมาแก้ทีหลังมักจะ "ปรับให้ง่ายขึ้น" แล้วทำพัง

1. **`LockService` ใน GAS** (`google_apps_script.js:47`) — กัน 2 เครื่องยิงพร้อมกันแล้วชีตเพี้ยน
2. **upsert ด้วย bill ID** — ค้นก่อนเขียนเสมอ ส่งซ้ำกี่รอบก็ได้แถวเดียว
3. **`cloudOutbox` แบบ persist + backoff** — ปิดกะตอนเน็ตหลุดแล้วส่งเองทีหลังได้จริง
4. **กัน race ด้วย `tx.rev`** — บิลที่ถูกแก้ระหว่าง sync จะไม่ถูก mark synced ทับ จุดนี้ละเอียดมาก คนส่วนใหญ่พลาด
5. **เช็ค `includes(tx)` ทั้งก่อนและหลัง `await`** — บิลที่ถูกลบระหว่างส่งจะไม่ขึ้นชีต
6. **`escapeHtml()` ครบทุกจุดที่แสดงชื่อผู้ใช้** — ตรวจแล้ว 25/25 จุด ไม่มีรูรั่ว
7. **`safeCell()` กัน formula injection ในชีต** — สำคัญและมักถูกลืม
8. **ตรวจชนิดข้อมูลตอน import** ไม่ใช่แค่เช็ค truthy
9. **รับเฉพาะ `data:image/` สำหรับโลโก้** — กัน JSON แปลกปลอมฝังสคริปต์
10. **`requestPersistentStorage()`** — ขอไม่ให้ iOS ลบ IndexedDB ทิ้ง
11. **service worker ไม่ `skipWaiting()` เอง** — ต้องให้พนักงานกดยืนยัน ไม่รีเฟรชทับกลางบิล
12. **ปุ่มจบบิล `disabled = true` ทันทีที่กด** — กดรัวไม่เกิดบิลซ้ำ

---

# ส่วนที่ 5 — ลำดับที่แนะนำให้ทำ

## ทำก่อนใช้งานต่อ
| # | รายการ | บรรทัด | เหตุผล |
|---|---|---|---|
| 1 | **[C-1]** ธง `loadFailed` บล็อก `saveState` | ~15 | ตัวเดียวที่ทำเจ๊งได้ |
| 2 | **[H-1]** เก็บคำสั่งลบแถวใน `clearSalesData` | ~4 | ตัวเลข 2 ที่ไม่ตรงกันหาสาเหตุยากมาก |
| 3 | **[M-4]** ปุ่มไอคอน 44px | 1 | กระทบพนักงานทุกวัน แก้บรรทัดเดียว |

**รวม ~20 บรรทัด**

## ทำเมื่อสะดวก
| # | รายการ | บรรทัด |
|---|---|---|
| 4 | **[H-2]** ยืนยันซ้ำใน `resetData` | ~10 |
| 5 | **[H-3]** เขียนขั้นตอนกู้ข้อมูลใส่คู่มือ | — |
| 6 | **[M-1]** ย้าย `archiveOldData` ออกจาก `saveState` | ~5 |

## ทำเมื่อเห็นอาการ
| # | รายการ | สัญญาณที่บอกว่าถึงเวลา |
|---|---|---|
| 7 | **[M-3]** วาดเฉพาะหน้าที่เปิดอยู่ | กดจบบิลแล้วรู้สึกหน่วง |
| 8 | **[M-2]** แยกตาราง transactions | แก้ข้อ 7 แล้วยังหน่วง |
| 9 | **[F-3]** ย้ายแท็บเก่าไปไฟล์แยก | Sheets เปิดช้า (ปีที่ 3-4) |

---

# ภาคผนวก — งานที่แก้ไปในรอบนี้

**เพิ่มการรายงานเงินขาด/เกินตอนปิดกะขึ้น Google Sheet**

| ไฟล์ | สิ่งที่เพิ่ม |
|---|---|
| `app.js` | `getClosedShiftsForPeriod()`, `buildShiftCashSummary()`, ฟิลด์ `cashVariance / shiftCount / shiftCash[]` ใน payload |
| `google_apps_script.js` | คอลัมน์ที่ 7 ในชีต `สรุปรายเดือน`, บล็อก "การนับเงินสดปิดกะ" ในชีตรายวัน, `migrateMasterAddVarianceColumn()`, `shiftRangeLabel()` |

**ผ่านการทดสอบ:** `node --check` ทั้ง 2 ไฟล์ + edge case 11 ข้อ (กะคร่อมเที่ยงคืน, กะข้ามเดือน, 2 กะวันเดียว, กะเก่าไม่มี `expectedCash`, ฟิลด์ `undefined`/`NaN`, กะไม่มี timestamp)

**⚠️ ยังไม่มีผลจนกว่าจะ deploy Apps Script**
Apps Script → วางโค้ดใหม่ → **Deploy > Manage deployments > แก้ version เป็น New version**
(ถ้ากด Deploy ใหม่เฉย ๆ จะได้ URL ใหม่แล้วแอปยิงไปที่ตัวเก่า)

**ข้อจำกัดที่ยอมรับไว้:** ปิด 2 กะในวันทำการเดียวกัน master sheet จะโชว์ยอดสุทธิ (ขาด 100 + เกิน 100 = 0) รายละเอียดรายกะดูได้ในชีตรายวัน ซึ่งอยู่ 62 วัน
